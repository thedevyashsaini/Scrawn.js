{
  "name": "@scrawn/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/@scrawn/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "bun-types": "^1.3.1",
    "typescript": "^5.3.0"
  }
}
